apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nki-mysql-pvc
  namespace: nki-database
  labels:
    k8s-app: nki-mysql

spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  volumeName: nki-mysql-pv
