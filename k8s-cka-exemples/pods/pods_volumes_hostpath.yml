apiVersion: v1
kind: Pod
metadata:
  name: volume-ephemeral-pod

spec:
  containers:
  - name: redis-container
    image: redis
    volumeMounts:
    - name: "hostpath-volume"
      mountPath: "/datas"

  volumes:
  - name: "hostpath-volume"
    hostPath:
      path: "/var/lib/2-persist"